schemaVersion: "3.0"
name: User API - Integration Test
description: This is a User API for demoing V12
variables:
  baseUrl: /
auth:
  - id: ""
    type: apikey
    name: apikey auth
    credentials:
      key: X-API-Key
      value: "{{apiKey}}"
      in: header
